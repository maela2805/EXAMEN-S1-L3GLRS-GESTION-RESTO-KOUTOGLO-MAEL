{% extends 'gestionnaire/_layout.html.twig' %}

{% block title %}Modifier un quartier{% endblock %}

{% block content %}
<div class="d-flex justify-content-center align-items-center" style="min-height:80vh">
    <div class="card shadow-sm card-form">
        <div class="card-body p-4">

            <h4 class="text-center mb-4 fw-bold">
                ✏️ Modifier le quartier
            </h4>

            {{ form_start(form) }}

            <div class="mb-3">
                {{ form_label(form.libelle) }}
                {{ form_widget(form.libelle, {'attr': {'class': 'form-control'}}) }}
            </div>

            <div class="mb-4">
                {{ form_label(form.zone) }}
                {{ form_widget(form.zone, {'attr': {'class': 'form-select'}}) }}
            </div>

            <div class="d-flex justify-content-between">
                <a href="{{ path('gestionnaire_quartiers') }}"
                   class="btn btn-outline-secondary">
                    Annuler
                </a>

                <button class="btn btn-warning">
                    Modifier
                </button>
            </div>

            {{ form_end(form) }}

        </div>
    </div>
</div>
{% endblock %}
